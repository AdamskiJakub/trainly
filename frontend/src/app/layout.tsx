// Since we have a root `[locale]` segment, this file is required
// to customize the root layout. Note that this doesn't add an
// additional root layout, it just provides a way to customize
// the root HTML element.

export { default } from './[locale]/layout';
